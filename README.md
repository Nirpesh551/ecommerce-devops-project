# E-Commerce DevOps Project

A 3-tier e-commerce app built with React (frontend), Node.js/Express (backend), MongoDB (DB). Containerized with Docker, CI/CD via GitHub Actions, deployed to Ubuntu server, monitored with Prometheus/Grafana.

## Setup
- Clone repo: `git clone <url>`
- Follow steps in this README.

## Architecture

